export default function Button({ buttonLabel }) {
  return (
    <button className="btn-main">
      {/* Background layers */}
      <span></span>

      <span></span>

      {/* Border corners */}
      <span></span>
      <span></span>

      {/* Text */}
      {buttonLabel}
    </button>
  );
}
